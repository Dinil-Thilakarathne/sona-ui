---
title: "AI agents"
slug: ai-agents
description: "Connect coding agents to Sona UI's registry, documentation, and design guidance."
searchable: true
---

# AI agents

Use Sona UI with coding agents that can inspect a project, select a suitable component, and install its source through the registry. The agent should adapt the component to the project rather than treating it as an isolated snippet.

## How it works

<AgentTable
  rows={[
    {
      layer: "Registry",
      description:
        "Delivers source files, dependencies, and foundations through the Sona namespace.",
    },
    {
      layer: "Agent catalog",
      description:
        "Helps an agent select a component by intent, capability, accessibility, and motion.",
    },
    {
      layer: "Sona UI skill",
      description:
        "Teaches the agent how to inspect, install, compose, and validate components.",
    },
    {
      layer: "MCP",
      description:
        "Gives supported clients natural-language access to the shadcn registry.",
    },
  ]}
/>

## Recommended workflow

1. Connect the registry to the project where the agent will work.
2. Install the Sona UI skill.
3. Describe the interaction you need, not just a component name.
4. Review the agent's selection and explanation.
5. Install the component and validate the consumer project.

## Configure the registry

Add the Sona UI registry to the components configuration file.

<CodeBlockWrapper
  code={`{
  "registries": {
    "@sona-ui": {
      "url": "https://sonaui.com/r/{name}.json"
    }
  }
}`}
  language="json"
  filename="components.json"
/>

## Connect MCP

The official shadcn MCP server is the supported path for natural-language registry discovery and installation. Sona UI does not require a custom MCP server.

<NewTabs defaultValue="codex">
  <TabsList>
    <TabsTrigger value="codex">Codex</TabsTrigger>
    <TabsTrigger value="claude">Claude Code</TabsTrigger>
    <TabsTrigger value="cursor">Cursor</TabsTrigger>
  </TabsList>
  <TabsContent value="codex">
    <CodeBlockWrapper
      code={`[mcp_servers.shadcn]
command = "npx"
args = ["shadcn@latest", "mcp"]`}
      language="toml"
      filename="~/.codex/config.toml"
    />
  </TabsContent>
  <TabsContent value="claude">
    <CodeBlockWrapper
      code={`{
  "mcpServers": {
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"]
    }
  }
}`}
      language="json"
      filename=".mcp.json"
    />
  </TabsContent>
  <TabsContent value="cursor">
    <CodeBlockWrapper
      code={`{
  "mcpServers": {
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"]
    }
  }
}`}
      language="json"
      filename=".cursor/mcp.json"
    />
  </TabsContent>
</NewTabs>

## Ask for a component

Use a request that states the interaction, expected behavior, and any constraints.

<CodeBlockWrapper
  code="Find an accessible tab component for switching between related settings. Do not install anything yet."
  language="text"
/>

<CodeBlockWrapper
  code="Inspect @sona-ui/animated-dialog, explain its accessibility and motion behavior, then install it without changing the existing page layout."
  language="text"
/>

## Verify the connection

Ask the agent to list the components available from the Sona UI registry and recommend the best choice for switching between related settings. It should explain the choice and link to the relevant documentation or registry item before it installs anything.

## Troubleshooting

If the registry is unavailable, confirm the components configuration, item URL placeholder, client configuration, and network access. When installation fails, first identify whether the issue belongs to consumer configuration, the registry payload, agent resources, or deployment.

## Resources

- [Agent catalog](/agent/catalog.json)
- [Full agent guidance](/llms-full.txt)
- [Registry index](/r/registry.json)
- [Raw Markdown documentation](/docs/installation/raw.md)

## Contact

Found a bug or need help using Sona UI with an agent? [Open an issue on GitHub](https://github.com/Dinil-Thilakarathne/sona-ui/issues).

## License & Usage

Sona UI is available under the MIT license. You may use, modify, and distribute components in personal and commercial projects.
