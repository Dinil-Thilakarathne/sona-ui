---
title: BubbleUpButton
slug: bubbleUpButton
tags: ["UI", "Button", "Animation", "Component"]
---

# BubbleUpButton Component

The `BubbleUpButton` component provides an interactive button with a fluid "bubble-up" animation effect when hovered. It creates an engaging user experience with minimal effort.

## Features

- Engaging hover animation with bubble-up effect.
- Customizable animation controls through motion props.
- Lightweight and reusable.
- Preserves text legibility during animation using mix-blend-difference.
- Built with Framer Motion for smooth animations.

<Divider />

<Tabs
  tabs={[
    {
      title: "Preview",
      content: <ComponentPreview componentPath="BubbleUpButton_ex.tsx" />,
    },
    {
      title: "Usage",
      content: (
<CodeSyntaxHighlighter
  language="tsx"
  code={
`import BubbleUpButton from "../sonaui/bubbleUpButton/BubbleUpButton";

// Basic usage

<BubbleUpButton>Hover me!</BubbleUpButton>

// With custom motion controls

<BubbleUpButton
  motionControls={{
    transition: {
      duration: 1,
      type: "spring",
      stiffness: 400,
      damping: 30,
    },
  }}
>
  Custom Animation
</BubbleUpButton>

// With additional button props

<BubbleUpButton onClick={() => alert("Button clicked!")} className="my-4">
  Interactive Button
</BubbleUpButton>
` }/> ), },

{
title: "CLI",
content: <CodeSyntaxHighlighter language="js" code={`npx @sonacode/sonaui-cli add BubbleUpButton`}/>
}
]} />

## Source code

<Tabs
  tabs={[
    {
      title: "BubbleUpButton.tsx",
      content: (
        <CodeSyntaxHighlighter
          language="tsx"
          filePath={{
            directory: "bubbleUpButton",
            fileName: "BubbleUpButton",
          }}
        />
      ),
    },
  ]}
/>
{/* 
## How It Works

The BubbleUpButton uses Framer Motion to create a fluid animation effect:

1. The button has a base black background with a white overlay
2. On hover, the white overlay clips into view from the bottom
3. The text uses `mix-blend-difference` to maintain visibility during the animation
4. On mouse leave, the animation reverses and the overlay resets

This creates a smooth, interactive "bubble up" effect that engages users. */}

## Props

<PropTable
  data={[
    {
      name: "children",
      type: "ReactNode",
      default: "'Hover me!'",
      description: "The content to be displayed inside the button.",
    },
    {
      name: "motionControls",
      type: "MotionConfigProps",
      default:
        "{ transition: { duration: 2, type: 'spring', stiffness: 300, damping: 40 } }",
      description:
        "Custom Framer Motion configuration to control the animation behavior.",
    },
    {
      name: "className",
      type: "string",
      default: "undefined",
      description:
        "Additional CSS classes for the button element. These will be merged with the component's default classes using the cn utility.",
    },
    {
      name: "...props",
      type: "ButtonHTMLAttributes<HTMLButtonElement>",
      default: "undefined",
      description: "All standard button HTML attributes are supported.",
    },
  ]}
/>

{/* ## Customization

You can customize the BubbleUpButton in several ways:

1. **Animation Behavior**: Adjust the `motionControls` prop to change the speed and feel of the animation
2. **Styling**: Use the `className` prop to modify the button's appearance
3. **Content**: Add text, icons, or any other content as children
4. **Button Behavior**: All standard button props like `onClick`, `disabled`, etc. are supported

For best results, maintain the overflow and positioning properties as they're essential for the animation effect. */}
