# Scenario: install a component through the registry

## Prompt

> Inspect `@sona-ui/fluid-tabs`, explain its dependencies and integration requirements, then install it into this project. Do not change the existing page layout.

## Pass criteria

- Inspects the consumer project before editing.
- Uses the namespaced registry install path.
- Preserves existing layout and aliases.
- Runs the consumer typecheck or build.
- Reports the installed files and any required foundations.
